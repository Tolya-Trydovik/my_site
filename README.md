# my_site
Django Project

## Before first launch
1. python3 -m venv env
2. . env/bin/activate
3. pip install -r requirements.txt
4. Configure project using `local_setings.dev.py` as example.
5. Your local settings should be written in `local_setings.py`
6. python manage.py migrate